apiVersion: 1

providers:
  - name: MediaFusion
    orgId: 1
    folder: MediaFusion
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
